"""
Verschachtelte Listen in Python

Verschachtelte Listen sind Listen, die andere Listen als Elemente enthalten.
Sie sind nützlich für die Darstellung und Verarbeitung von mehrdimensionalen
Daten, wie Matrizen oder Tabellen.

Themenübersicht:
1. Erstellung und Zugriff auf verschachtelte Listen
2. Iteration über verschachtelte Listen
3. Anwendung: Matrixoperationen
4. Beispiel: Verarbeitung von tabellarischen Daten
"""

# 1. Erstellung und Zugriff auf verschachtelte Listen
# Eine verschachtelte Liste kann wie jede andere Liste erstellt werden.
matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9],
]

# Zugriff auf Elemente innerhalb der verschachtelten Liste

# Änderung eines Werts

# 2. Iteration über verschachtelte Listen
# Iteration über die äußere Liste

# Iteration über alle Elemente (mit einer verschachtelten Schleife)

# 4. Beispiel: Verarbeitung von tabellarischen Daten
# Darstellung einer Tabelle mit verschachtelten Listen
students = [
    ["Name", "Alter", "Klasse"],
    ["Anna", 10, "5A"],
    ["Ben", 12, "6B"],
    ["Clara", 11, "5A"],
]

# Zugriff auf spezifische Daten

# Ausgabe der Tabelle mit Formatierung

# Aufgabe: Erstellen Sie eine verschachtelte Liste, die die Monatsumsätze jan - march
# von drei Filialen A, B und C speichert. Implementieren Sie:
# 1. Die Ausgabe aller Umsätze pro Filiale.
# 2. Die Berechnung des Gesamtumsatzes pro Filiale.
# 3. Die Berechnung des höchsten Gesamtumsatzes unter allen Filialen.
# A: [34, 14, 19]
# B: [134, 314, 9]
# C: [4, 114, 139]
#
Monatsumsätze = [
    [34, 14, 19],
    [134, 314, 9],
    [4, 114, 139],
]
